public class operators {
    public static void main(String[] args) {
        

    // OPERATORS

        // 1) Arithmatic Operators (binary: +,-,*,/,%  unary: ++,--)
        // 2) Relational Opereators (>,<,>=,<=,==,!=)
        // 3) Logical Operators (&&,||,!)
        // 4) Bitwise Operators (&, |,^(xor),>>(binary left shift),<<(binary left shift))
        // 5) Assignment Operators (=, +=, -=, *=, /=)

        // 1) Arithmatic operators 
        //     -binary
                int a=10;
                int b=20;
                int c=a+b;
                System.out.println(c);
        //     -unary;
                b=a++;
                System.out.println(a);
                System.out.println(b);


    }
}
